# Copy to .env (git-ignored). Nothing here is a real credential.

# The mock bank's synthetic operator sign-on, and the handles the artifact names.
CUA_OPERATOR_USERNAME=opsuser
CUA_OPERATOR_PASSWORD=synthetic-not-a-real-password
CUA_SECRET_CORE_OPERATOR_USERNAME=opsuser
CUA_SECRET_CORE_OPERATOR_PASSWORD=synthetic-not-a-real-password

# Recording only. Replay needs neither of these.
# ANTHROPIC_API_KEY=sk-ant-...
# OPENROUTER_API_KEY=sk-or-...
